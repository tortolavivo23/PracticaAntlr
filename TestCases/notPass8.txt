PROGRAM Hello;
VAR
Nota: integer;

FUNCTION funcionAuxiliar(numero: integer; numeroReal: real; cont: integer);
BEGIN
    numero := numeroReal * (numero + 2);
    WHILE (cont < "3") DO BEGIN
        funcionAuxiliar(numero, numeroReal, cont + 1);
    END
END;

BEGIN
    Asignado := 1;
    REPEAT
      BEGIN
          Nota := Nota + 1;
      END
    UNTIL (Nota = 10);
    writeln("La nota es", funcionAuxiliar(Nota, 3.05E12)); { Cálculo de la nota }
    writeln("Nota");
END.
